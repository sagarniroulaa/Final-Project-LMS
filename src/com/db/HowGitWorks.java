package com.db;

/*
    Repository, Staging Area, Commit, GitHub

    git init -> turns a normal folder into a Git repository.

    SA -> Waiting room , Files go into staging before they are permanently saved
            into history.

    Commit -> A snapshot of our project at a given moment.
            Each commit has a message that describes what has changed.
            ->
            Please tell me who you are.

            git config --global user.email "you@example.com"
            git config --global user.name "Your Name"

    GitHub -> A cloud storage for Git repositories.
            -> Lets you share and collaborate with others.
 */

public class HowGitWorks {
}
